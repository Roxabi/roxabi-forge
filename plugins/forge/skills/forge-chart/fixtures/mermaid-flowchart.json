{
  "skill": "forge-chart",
  "variant": "mermaid-flowchart",
  "input": {
    "prompt": "Draw a Mermaid flowchart LR showing: client → API gateway → auth service → database, with a sidecar cache between gateway and database.",
    "slug": "api-auth-flow",
    "project": "_shared/diagrams"
  },
  "expected_output_hash": null,
  "notes": "Baseline Mermaid flowchart LR — 5 nodes, one sidecar edge. Anchors the dynamic-tabs Mermaid pattern from references/mermaid-guide.md."
}
