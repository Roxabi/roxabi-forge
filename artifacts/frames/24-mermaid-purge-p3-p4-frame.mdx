---
title: Mermaid purge P3+P4 — delete sources + CLAUDE.md + close #19 + blocking grep guard
issue: 24
status: approved
tier: F-lite
date: 2026-04-15
parent: 21
---

## Problem

Cycle 2 of the 4-phase Mermaid purge (#21) is nearly complete. P1 (#22) shipped 6 native fgraph templates. P2 (#23) migrated 5 skills + 3 scripts off Mermaid. P3+P4 remain: delete all Mermaid source files, update the two CLAUDE.md decision rules, close #19 (superseded hardening issue), and wire a blocking grep guard that prevents re-introduction.

The Mermaid CDN dependency (~900 KB runtime) is still present on disk via `*-mermaid.html` legacy files and `mermaid-init.js`. The decision rules in `~/.claude/CLAUDE.md` and `~/projects/CLAUDE.md` still suggest "> 8 nodes → Mermaid" — a stale recommendation that must be removed to prevent future authors from reintroducing the pattern.

## Who

- **Primary:** Mickael (forge author) — gains a clean codebase with no Mermaid vestiges; the blocking guard enforces the new default.
- **Secondary:** downstream artifact consumers — all outputs are now offline-safe, no CDN dependency, consistent theming via `fgraph-base.css`.
- **Tertiary:** future forge contributors — the CI guard blocks Mermaid additions, making the architectural decision visible in PR review.

## Constraints

- **Dependency:** #23 (P2 migrations) must be merged — verified: artifacts/plans/23-mermaid-purge-p2-migrate-plan.mdx exists.
- **Guard scope:** `artifacts/` excluded from grep guard so historical spec/plan references don't trigger CI failure.
- **Outside-repo files:** `~/.claude/CLAUDE.md` and `~/projects/CLAUDE.md` are not tracked; changes delivered via PR post-merge checklist.
- **Error-message exemption:** capitalized `Mermaid` in render-script error prose (P2.7/P2.8) must not trigger the lowercase word-boundary grep — use `\bmermaid\b`.

## Out of Scope

- P1 (template build) — shipped in #22.
- P2 (skill/script migration) — shipped in #23.
- New fgraph templates — all 6 exist.
- PNG/PDF export (#14), inline-SVG types (#20), auto-layout (#7).
- SRI hardening on remaining CDN assets (orthogonal; `svg-pan-zoom` if still present).

## Complexity

**Tier: F-lite** — bounded scope (deletions + config), single domain, clear predecessor, no unknowns.

Signals:
- Clear deliverables from parent spec § P3, § P4
- ~9 files touched, but straightforward deletions + config edits
- Pattern established by #22 and #23
- No architectural decisions remaining
