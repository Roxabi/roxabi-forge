# Patterns

Naming conventions, error handling, and data flow patterns used across the project.

## Naming Conventions

<!-- Document naming rules. Examples:
  | Context | Convention | Example |
  |---------|-----------|---------|
  | Files | kebab-case | `my-component.ts` |
  | Variables | camelCase | `userName` |
  | Constants | UPPER_SNAKE | `MAX_RETRIES` |
-->

TODO: Document naming conventions.

## Error Handling

<!-- Document your error strategy. Examples:
  - Custom error classes: `AppError`, `ValidationError`, `NotFoundError`
  - Error codes: `AUTH_001`, `VALIDATION_002`
  - HTTP mapping: 400=validation, 401=auth, 404=not-found, 500=internal
-->

TODO: Document error handling patterns.

## Data Flow

<!-- Document how data moves through the system. Example:
  Request → Controller → Service → Repository → Database
  Response ← Controller ← Service ← Repository ← Database
-->

TODO: Document data flow.
