__pycache__/
*.pyc
.venv/
.env
.env.local
node_modules/
.cache/

# pnpm (local tooling only — repo uses bun)
pnpm-lock.yaml
pnpm-workspace.yaml

# Dev-core (local overrides — stack.yml is committed for CI context-lint)
.claude/dev-core.yml
.claude/scheduled_tasks.lock

# Keep example
!.claude/stack.yml.example
# CocoIndex Code (ccc)
/.cocoindex_code/

# forge-chart QA loop (transient harness + generated artifacts) — local-only, never tracked
.claude/fc-loop/

# Generated QA frames (screenshots/renders) — local-only output
/artifacts/frames/

# Impeccable design-hook cache — local PostToolUse artifact, never part of the plugin
.impeccable/
